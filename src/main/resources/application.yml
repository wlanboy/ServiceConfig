spring:
  application:
    name: serviceconfig
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: ${CONFIG_LOCATION:file:////config }
    
server:
  port: ${PORT:8888}
 

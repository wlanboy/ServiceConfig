spring:
  application:
    name: serviceconfig
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: ${CONFIGLOCATION:file:////config }
    
server:
  port: ${PORT:8888}
 
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: ${EUREKAZONE:http://127.0.0.1:8761/eureka/}
  instance:
      hostname: ${EUREKAHOST}
